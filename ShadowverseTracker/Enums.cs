namespace ShadowverseTracker
{
    public enum Craft { All, Forest, Sword, Rune, Dragon, Shadow, Blood, Haven };

    public enum Mode { All, Ranked, Unranked, TakeTwo };

    public enum ViewMode { Stats, Decks, Matchups };

    public enum Turn { All, First, Second };
}